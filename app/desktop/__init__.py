"""Desktop application layer — a local GUI over the scrapers.

Imports nothing from ``agent/`` and needs no server, database or API key: the
whole app is the scrapers in ``app/scrapers`` plus ``app.seen_store``.
"""
